# Core dependencies for running on GCP VMs with CUDA GPU.
# Install with:  pip install -r requirements-gcp.txt

# PyTorch (CUDA 12.1) – adjust the index URL for your CUDA version.
--extra-index-url https://download.pytorch.org/whl/cu121
torch
torchaudio
torchvision

# Scientific stack
numpy
pandas
scipy
scikit-learn
matplotlib
seaborn

# Jupyter (for running notebooks via JupyterLab or VS Code)
jupyterlab
ipykernel

# Neural data dependency (used by create_neural_targets_from_psth)
spks @ git+https://github.com/spkware/spks.git
natsort
tqdm
h5py

# This repo itself (installs src/ as a package)
-e .
